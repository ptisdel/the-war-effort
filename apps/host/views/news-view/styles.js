import styled from 'styled-components';

export const Root = styled.div(() => ({
  background: 'gray',
  bottom: 0,
  left: 0,
  padding: '40px',
  position: 'absolute',
  right: 0,
  top: 0,
}));

export const PlayerList = styled.div(() => ({
  left: 0,
  padding: '40px',
  position: 'absolute',
  top: 0,
}));
