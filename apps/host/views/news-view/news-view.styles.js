import styled from 'styled-components';

export const Root = styled.div(() => ({
  background: 'gray',
  height: '100%',
  width: '100%',
}));

export const PlayerList = styled.div(() => ({
  left: 0,
  padding: '40px',
  position: 'absolute',
  top: 0,
}));
