import styled from 'styled-components';

export const Root = styled.div(() => ({}));

export const PublicSupport = styled.div(() => ({}));

export const Campaigns = styled.div(() => ({}));

export const NewCampaignButton = styled.div(() => ({}));
