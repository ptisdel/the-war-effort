import styled from 'styled-components';

export const Root = styled.div(() => ({
}));

export const Airbase = styled.div(() => ({
}));

export const AirbaseName = styled.div(() => ({
}));

export const AircraftList = styled.div(() => ({
}));

export const Aircraft = styled.div(() => ({
  backgroundColor: 'lightgray',
  cursor: 'pointer',
  display: 'inline-flex',
  marginLeft: '10px',
  padding: '5px',
}));

export const AircraftName = styled.div(() => ({
}));

export const AircraftAmmo = styled.div(() => ({
}));
