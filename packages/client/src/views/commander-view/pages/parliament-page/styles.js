import styled from 'styled-components';

export const Root = styled.div(() => ({
}));

export const ParliamentSentiment = styled.div(() => ({

}));

export const Budget = styled.div(() => ({
}));

export const RequestButton = styled.div(() => ({
}));
