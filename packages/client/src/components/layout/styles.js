import styled from 'styled-components';

export const Root = styled.div(() => ({
  position: 'absolute',
  left: '0',
  right: '0',
  top: '0',
  bottom: '0',
}));

export const Main = styled.div(() => ({
  height: '100%',
  width: '100%',
}));
